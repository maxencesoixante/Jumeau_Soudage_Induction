__pycache__/
*.pyc
.venv/
venv/
.idea/
.DS_Store
*.egg-info/
build/
dist/
resultats/
donnees/resultats/
donnees/journaux/resultats_calibration.log
donnees/journaux/resultats_validation.log
donnees/journaux/resultats_validation_thetafinal.log
donnees/journaux/resultats_validation_2d.log
donnees/journaux/resultats_validation_2d_thermostat.log
donnees/journaux/resultats_validation_2d_final.log
donnees/journaux/resultats_validation_2d_newpos.log
.pytest_cache/

# Couche IA (secrets et configs de session éphémères)
ai_framework/.env
config/essais/_session_*.yaml

# vidéos brutes caméra thermique (volumineuses ; garder hors dépôt)
donnees/data/**/*.mp4

# diapos de réunion (volumineux ; garder hors dépôt) — tout sous-dossier de biblio/
biblio/**/*.pptx

# vidéos de présentation (lourdes) — hors VCS comme les pptx
biblio/**/*.mp4

# exports vectoriels des figures (générés à la demande via FIG_FORMATS, #19) —
# seul le PNG est versionné ; PDF/TIFF sont produits pour la soumission article
docs/figures/*.pdf
docs/figures/*.tif
docs/figures/*.tiff

# SDD scratch (subagent-driven-development ledger/artefacts)
.superpowers/
